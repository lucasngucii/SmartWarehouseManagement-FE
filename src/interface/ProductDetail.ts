export default interface ProductDetail {
    supplierId: string
    colorId: string
    sizeId: string
    materialId: string
    brandId: string
    dimension: string
    weight: number
    description: string
}